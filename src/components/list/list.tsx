import React, { FC, PropsWithChildren } from 'react'
import './list.css';

const List: FC = ({ }: PropsWithChildren): JSX.Element => {
    return (
        <div>

        </div>
    )
}
export default List