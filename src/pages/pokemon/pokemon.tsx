import React, { FC, PropsWithChildren } from 'react'
import './pokemon.css'
import { PokemonProps } from './pokemon.types'

const Pokemon: FC<PokemonProps> = ({ }: PropsWithChildren<PokemonProps>): JSX.Element => {
    return (
        <main>

        </main>
    )
}
export default Pokemon